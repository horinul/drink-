<view class="com">
    <view class="title"></view>
    <view class="subTitle"></view>
    <view class="teaList">
        <view class="teaItem" wx:for="{{tesList}}" wx:for-item="item" bind:tap="toOther" data-url="{{item.appid}}">
            <image class="img"  src="{{item.imgUrl}}" alt=""/>
            <view class="text">{{item.text}}</view>
        </view>
    </view>
    <view class="kon"></view>
</view>